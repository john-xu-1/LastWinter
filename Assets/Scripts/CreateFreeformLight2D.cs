using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateFreeformLight2D : MonoBehaviour
{
    public UnityEngine.Experimental.Rendering.Universal.Light2D FreeformLight;
    // Start is called before the first frame update
    void Start()
    {
       // FreeformLight.po
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
